import type { BlockStyle } from 'core-blocks';

const variations: BlockStyle[] = [
	{
		name: 'default',
		label: 'Default',
		isDefault: true,
	},
	{
		name: 'narrow-text',
		label: 'Narrow',
		isDefault: false,
	},
];

export default variations;
