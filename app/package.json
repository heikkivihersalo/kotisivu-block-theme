{
  "name": "kotisivu-block-theme",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "preview": "npm-run-all --parallel sync start",
    "sync": "browser-sync start -p 'domain.tld' --files '**/*.php' 'style.css' 'build/*.js' 'build/*.css'",
    "build": "wp-scripts build --config webpack.config.js",
    "start": "wp-scripts start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@wordpress/scripts": "^24.0.0",
    "browser-sync": "^2.27.10",
    "firebase": "^9.9.3",
    "mini-css-extract-plugin": "^2.6.1",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "firebase": "^9.3.0"
  }
}
